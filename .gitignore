# Xcode build products and derived data
build/
DerivedData/
*.xcarchive

# Xcode user-specific state (not meant to be shared)
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# Packaged app output
*.dmg
dist/

# macOS
.DS_Store

# Demo/screen recordings kept out of the repo (GitHub has a 100MB limit) —
# except the small README demo GIF, which is meant to be committed
*.mov
*.mp4
*.gif
!demo.gif
